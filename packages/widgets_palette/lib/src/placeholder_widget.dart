import 'package:flutter/material.dart';

class PalettePlaceholder extends StatelessWidget {
  const PalettePlaceholder({super.key});

  @override
  Widget build(BuildContext context) =>
      Container(color: Colors.grey.shade200, height: 80, width: 80);
}
